apiVersion: v1
kind: Secret
metadata:
  name: firefly-iii-secrets
  namespace: firefly-iii
type: Opaque
stringData:
  # IMPORTANT: Change these values in production!
  APP_KEY: "SomeRandomStringOf32Chars12345" # Must be exactly 32 characters
  DB_PASSWORD: "firefly-db-password"
  STATIC_CRON_TOKEN: "your-cron-token-here"
