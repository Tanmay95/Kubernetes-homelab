apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ns.yaml
  - secret.yaml
  - deployment-db.yaml
  - deployment.yaml
  - deployment-importer.yaml
  - service.yaml

# NOTE: HTTPRoutes are intentionally not applied here.
# This overlay is configured for internal-only access (use `kubectl port-forward`).

namespace: firefly-iii
